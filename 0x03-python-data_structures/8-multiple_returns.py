#!usr/bin/python3
def multiple_returns(sentence):
    length = len(str)
    First_character = str[0]
    if length == 0:
        print('First character = {}'.format(None))
    else:
        print("Length: {:d} - First character: {}".format(length, First_character))
